import 'package:chatbot_allen/constants/apiKeys.dart';import 'package:google_generative_ai/google_generative_ai.dart';class GeminiAIService{  //Checking if user wants to generate an Image or Text Response  Future<String> isArtPromptAPI(String prompt)async{    try{      final model = GenerativeModel(model: 'gemini-1.5-flash', apiKey: GeminiAIapiKey);      final content = [Content.text('Is this provided prompt wants to generate or create any image? "$prompt", just reply only yes or no')];      final response = await model.generateContent(content);      print(response.text);      return response.text.toString();    }catch(e){      return e.toString();    }  }  Future<String> ChatGPTApi(String prompt)async{    return '';  }  Future<String> dallEAPI(String prompt)async{    return '';  }}